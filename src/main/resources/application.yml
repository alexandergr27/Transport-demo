spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transportdb
    username: develop
    password: d3v3l0p
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
